export const connect_end_menu = $state({
  show: false,
  source: "",
  top: 0,
  left: 0,
  last_open: 0,
});

export const node_context_menu = $state({
  show: false,
  source: "",
  top: 0,
  left: 0,
});
