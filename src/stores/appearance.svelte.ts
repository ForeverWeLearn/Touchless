export const theme = $state({
  default: "dark",
  current: "dark",
  list: ["dark", "light"],
});

export const layout = $state({
  default: "flex",
  current: "flex",
  list: ["flex", "chaos"],
});
