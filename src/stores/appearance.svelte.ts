export const theme = $state({
  default: "light",
  current: "light",
  list: ["dark", "light"],
});

export const layout = $state({
  default: "flex",
  current: "flex",
  list: ["flex", "chaos"],
});
